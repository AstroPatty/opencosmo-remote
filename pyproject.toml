[project]
name = "opencosmo-remote"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Patrick Wells", email = "patrick.wells.95@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "opencosmo>=0.9.6",
    "protobuf>=6.33.0",
]

[project.scripts]
opencosmo-remote = "opencosmo_remote:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
